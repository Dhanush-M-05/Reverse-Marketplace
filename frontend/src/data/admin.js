// Admin tables are derived at runtime from the live data store (DataContext)
// and the user registry (AuthContext). No seed data.
export const adminUsers = []
export const adminRequirements = []
export const adminQuotes = []
export const disputes = []
