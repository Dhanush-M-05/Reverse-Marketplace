// The user registry (including demo logins and runtime registrations) lives in
// AuthContext / localStorage. No seed marketplace users here.
export const users = []

// Recommended sellers shown in AI features are derived from approved sellers
// in the registry at runtime.
export const sellersDirectory = []
